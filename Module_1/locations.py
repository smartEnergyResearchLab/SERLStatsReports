""" 
Where are the datasets we'll be using?

Replace the stars with the paths for the SERL Observatory version described below in serl_data_version.
"""
serl_data_version = '****'
serl_data_path = ''
participant_data_file = r'****'
survey_data_file = r'****'
survey_dict_file = r'****'
bst_dates = r'****'
epc_file = r'****'
energy_daily_directory = r'****'
energy_daily_regexp = '****'
energy_hh_directory = r'****'
energy_hh_regexp = '****'
climate_directory = r'****'
